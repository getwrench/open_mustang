name: mustang_core
description: >
  A framework to build Flutter applications. It provides state management, persistence, caching and
  opinionated project structure.

version: 1.0.4
repository: https://github.com/getwrench/open_mustang

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  build_resolvers: ^2.0.4
  connectivity_plus: ^2.2.0
  flutter:
    sdk: flutter
  hive: ^2.0.4

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter
